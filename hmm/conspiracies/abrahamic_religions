יַהְוֶה‬‬ (yhwh ∨ yahweh ∨ jah ∨..)
الله‎ (allāh) …i do not yet understand the avoidance of depiction ↓ nomenclature; christians also worship the faceless many-faced god (jesus's jewish, so that's actually ⊥)

(:he(,6999):he(,7001)) abraham ∧ muslim
(:he(,8999):he(,9001)) moses ∧ jew ∧ 10c
(:he(,9999):he(,10001)) jesus ∧ christian ∧ bible
(:he(,10569):he(,10632,06,08)] muhammad ∧ islam ∧ quran
[:he(,11892,07,23):(,11975,08,27)] haile selassie i (ras tafari makonnen) ∧ rastafari

set-theoretic diagram; I should make this lisp-like https://en.wikipedia.org/wiki/DOT_(graph_description_language)
(define symbol /.*/) ; symbol is the default namespace
(hyperlink /.+/) ; "∞ " will be preppended, or " ∞" will be appended, to the namespace
(namespace /.*/)
(define a (namespace "abrahamic") (hyperlink "https://en.wikipedia.org/wiki/Abrahamic_religions"))
(define m (hyperlink "https://en.wikipedia.org/wiki/Muslim") (namespace "muslim"))
(define j (namespace "judaism"))
(define c (namespace "christianity"))
(define i (namespace "islamic"))
(define r (namespace "rastafarian")) ; they don't like "-ism"s
(superset subsets)
(a(m(j(c(r))) (i)))
; (f x y z), where x ∧ y ∧ z are subsets of f
; (a c) (b c), where a ∧ b intersect at c
; this tool will be `std`
; (), where an empty set will not be rendered, but anonymous sets just have an empty namespace
; …cfg…
define(symbol /.*/)
hyperlink(/.+/)
namespace(/.*/)
(a m(j(c(r)) i))
; f(x), where x is a subset of f
; (g f), where g ∧ f are subsets of the same superset
; a(c) b(c), where a ∧ b intersect at c
; this tool will be `std`
; (a(b (c d))), where c ∧ d are subsets of an anonymous subset of a (¬ subsets of b)
; …cfg: cfg will just be the first line as either "#!/usr/bin/env std --lisp", or some"-thing"

TODO:
	dictionary; interval notation for circa/floruit/tempore dating
		[a,b] = a - b
		(a,b] = c. (a+1) - b
		[a,b) = a - c. (b-1)
		(a,b) = c. (a+1) - c. (b-1)
		rexit can use curlies, but I'll only use it if I respect them; can't think of any off the top of my head…
	dictionary; latinisation
		l = latin = "engish"
		c̄ = cum = "with" (medicine)
		c. = circa = "around, about" ∨ "approximately"
		fl = floruit = "flourished"
		temp. = tempore = "in the time (of)" (why not just t. ?)
		r. = rexit = "reign"
		re = in re = "in the matter [of]"
